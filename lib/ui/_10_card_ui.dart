import 'package:flutter/material.dart';

class CardUI10 extends StatelessWidget {
  const CardUI10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CardUI10'),
      ),
    );
  }
}
