import 'package:flutter/material.dart';

class CardUI08 extends StatelessWidget {
  const CardUI08({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CardUI08'),
      ),
    );
  }
}
