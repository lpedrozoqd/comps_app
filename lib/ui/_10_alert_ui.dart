import 'package:flutter/material.dart';

class AlertUI10 extends StatelessWidget {
  const AlertUI10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AlertUI10'),
      ),
    );
  }
}
