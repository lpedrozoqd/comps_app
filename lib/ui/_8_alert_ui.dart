import 'package:flutter/material.dart';

class AlertUI08 extends StatelessWidget {
  const AlertUI08({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AlertUI08'),
      ),
    );
  }
}
